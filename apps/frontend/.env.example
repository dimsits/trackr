####################################
# API
####################################
# Base URL of the Trackr backend, including the global /api prefix.
# Must stay in sync with the backend PORT + API_PREFIX settings.
# Health probe used by the app resolves to <NEXT_PUBLIC_API_URL>/health
# -> http://localhost:3001/api/health
NEXT_PUBLIC_API_URL=http://localhost:3001/api
